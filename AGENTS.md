Immediately commit all changes, following Conventional Commits format.

Prefer inline styles, do not use CSS.

Decompose things into reusable components and files. Look for opportunities to
split thinigs proactively which seem like they will become too large in the
future.